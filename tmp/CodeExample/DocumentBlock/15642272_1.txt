            DocumentBlock block     = new DocumentBlock(stream, size);
            int           blockSize = block.size();
            if (block.partiallyRead())
            {
                break;
}