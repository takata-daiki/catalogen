public void wrapperMethod() {
          Table t = s.getTable(p);
          log.info("Found " + t.numRows() + "x" + cl
              + " table - TODO - convert");
          y += t.numParagraphs() - 1;
}