        for(XSLFShape shape : getShapeList()) {
          if (isCanceled.get()) {
            Log.d("Slide", "Thread.Canceled");
}
            shape.applyTransform(graphics);
            shape.draw(graphics);
